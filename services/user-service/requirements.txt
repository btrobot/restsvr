# FastAPI 用户服务依赖
# ==========================
# 核心框架
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0

# 数据库相关
# 同时支持 SQLite、PostgreSQL 和 MySQL
sqlalchemy==2.0.25
aiosqlite==0.19.0          # SQLite 异步驱动
asyncpg==0.29.0            # PostgreSQL 异步驱动（可选）
asyncmy==0.2.9             # MySQL 异步驱动（可选）

# Redis
redis==5.0.1

# 密码哈希
bcrypt==4.1.2

# CORS
python-multipart==0.0.6
python-jose[cryptography]==3.3.0

# HTTP 客户端（用于服务间调用）
aiohttp==3.9.1

# 监控和健康检查
psutil==5.9.8

# 日志
structlog==24.1.0

# 测试相关
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0
httpx==0.26.0              # FastAPI 测试客户端

# 工具库
python-dotenv==1.0.0

# 文档生成（OpenAPI）
openapi-python-client==0.15.2
